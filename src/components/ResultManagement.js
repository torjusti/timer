import React from 'react';

const ResultManagement = ( scramble, time ) => (
  <ul>
    <li>Time: {time}</li>
    <li>Scramble: {this.props.scramble}</li>
  </ul>
);

export default ResultManagement;
